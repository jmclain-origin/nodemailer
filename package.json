{
  "name": "nodemailer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "nodemailer": "^6.7.5"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "start": "set NODE_ENV=production && node index.js",
    "dev": "set NODE_ENV=development && nodemon index.js"
  }
}
